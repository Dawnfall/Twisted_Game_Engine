#pragma once
#include "twistedpch.h"
#include "EcsManager.h"


namespace Twisted::Transformations
{
	//Quat LookAt(const Vec3f& localPosition, const Vec3f& localLookAtPoint, const Vec3f& localUpVector, const EcsManager& ecs)
	//{
	//	Vec3f targetForward = glm::normalize(localLookAtPoint - localPosition);
	//	return glm::rotation(upVector, targetForward);
	//}

}